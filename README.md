# 💰 Expense Tracker - Python CLI

A simple **Command-Line Expense Tracker** built in Python to help you track your daily expenses.  
This program allows you to **add expenses**, **view all expenses**, and **calculate total spending**, with **persistent storage** in a CSV file.

---

## 🚀 Features

- Add expenses with **amount**, **category**, and **date**.
- Automatically validates input:
  - Amount must be positive.
  - Category cannot be empty and max 25 characters.
- View all expenses in a **formatted table**.
- Calculate **total spending**.
- Saves all data to a CSV file, so nothing is lost when you close the program.
- Interactive **animations** for loading, progress, and exiting.
- Handles invalid input gracefully.

---

## 📦 Installation

1. Clone this repository:

```bash
git clone https://github.com/YourUsername/Expense-Tracker.git
